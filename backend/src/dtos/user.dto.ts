export interface SignupDto {
    name: string;
    email: string;
    password: string;
}

export interface CreateUserRepositoryDto {
    userNumber: string;
    name: string;
    email: string;
    password: string;
}